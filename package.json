{
  "name": "israeli-whist-scorekeeper",
  "version": "1.0.0",
  "description": "Exact Bidding Score Engine & Companion for Israeli Whist (וויסט ישראלי)",
  "type": "module",
  "scripts": {
    "build": "esbuild src/js/app.js --bundle --minify --format=esm --outfile=src/js/app.bundle.js && esbuild src/css/styles.css --minify --outfile=src/css/styles.min.css",
    "test": "npm run build && node test-assets.js && node test-engine.js && node test-full-game.js && node test-sync-protection.js && node test-history-edit-baseline.js",
    "serve": "npx serve src -p 3000",
    "release": "bash scripts/release.sh",
    "deploy:dev": "bash scripts/deploy_dev.sh",
    "deploy:prod": "bash scripts/deploy_prod.sh"
  },
  "keywords": [
    "israeli-whist",
    "whist",
    "card-game",
    "scorekeeper",
    "bidding"
  ],
  "author": "Israeli Whist Community",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
